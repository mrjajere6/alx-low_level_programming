#include "main.h"

/**
 * add - adds to integerss together
 * and return its value
 * @n: parameter it accepts (an integer)
 * @x: 2nd parameter it accepts (also an integer)
 * Return: n + x
 */
int add(int n, int x)
{
	return (n + x);
}
