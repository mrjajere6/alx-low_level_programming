#include "main.h"

/**
 * _abs - prints absolute value of numbers put in it
 * @n: accepts an int argument
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	return (-n);
}
