#include "main.h"
#include <string.h>

/**
 * _strcat - concatinates the strings together
 * @dest: the destination of the string
 * @src: The source where the string will be added from
 * Return: a pointer
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
